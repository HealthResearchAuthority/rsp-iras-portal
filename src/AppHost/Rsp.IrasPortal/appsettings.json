{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "AppSettings": {
    "CategoriesServiceUri": "http://localhost:5067",
    "Authority": "https://accounts.google.com",
    "ClientId": "Set in secret store locally, using dotnet user-secrets, or KeyVault for other environments",
    "ClientSecret": "Set in secret store locally, using dotnet user-secrets, or KeyVault for other environments"
  }
}