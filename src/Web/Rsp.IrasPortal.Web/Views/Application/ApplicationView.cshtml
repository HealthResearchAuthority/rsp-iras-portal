@using Rsp.IrasPortal.Domain.Entities;
@using Rsp.IrasPortal.Application.DTOs.Responses;

@model IrasApplicationResponse

@{
    ViewData["Title"] = ViewBag.Title = "Application View";
}

<partial name="_BackNavigation" model="@("app:myapplications","Back", new Dictionary<string, string>())" />

@Html.DisplayForModel("Application")