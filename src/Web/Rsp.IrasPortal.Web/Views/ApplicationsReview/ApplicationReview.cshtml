@using Rsp.IrasPortal.Domain.Entities;
@using Rsp.IrasPortal.Application.DTOs.Responses;

@model IrasApplicationResponse

@{
    ViewBag.Title = "Application Review";
}

@* @section is added for the back button, so that we can render something below the back button
    and before the RenderBody() in the _Layout.cshtml. As RenderBody() renders anything that is not within
    the named section *@
@section BackNavigation {
    <partial name="_BackNavigation" model="@("arc:pendingapplications","Back", new Dictionary<string, string>())" />
}

@Html.DisplayForModel("Application")