using System.Text.Json;
using Microsoft.AspNetCore.Http;
using Microsoft.AspNetCore.Mvc;
using Microsoft.AspNetCore.Mvc.Rendering;
using Microsoft.AspNetCore.Mvc.ViewFeatures;
using Rsp.Portal.Application.Constants;
using Rsp.Portal.Application.DTOs;
using Rsp.Portal.Application.DTOs.CmsQuestionset;
using Rsp.Portal.Application.DTOs.CmsQuestionset.Modifications;
using Rsp.Portal.Application.DTOs.Responses;
using Rsp.Portal.Application.Responses;
using Rsp.Portal.Application.Services;
using Rsp.Portal.Web.Features.Modifications;

namespace Rsp.Portal.UnitTests.Web.Features.Modifications;

public class GetSpecificChangeByIdTests : TestServiceBase<ModificationsController>
{
    public GetSpecificChangeByIdTests()
    {
        var ctx = new DefaultHttpContext();
        Sut.ControllerContext = new ControllerContext { HttpContext = ctx };
        Sut.TempData = new TempDataDictionary(ctx, Mock.Of<ITempDataProvider>());
    }

    [Fact]
    public void GetSpecificChangesByAreaId_Should_Return_BadRequest_When_No_TempData()
    {
        // Arrange
        Mocker.GetMock<IApplicationsService>()
           .Setup(s => s.GetProjectRecord(It.IsAny<string>()))
           .ReturnsAsync(new ServiceResponse<IrasApplicationResponse> { StatusCode = HttpStatusCode.OK, Content = null });
        // Act
        var result = Sut.GetSpecificChangesByAreaId("A1");

        // Assert
        result.Result.ShouldBeOfType<StatusCodeResult>().StatusCode.ShouldBe(StatusCodes.Status400BadRequest);
    }

    [Theory, AutoData]
    public void GetSpecificChangesByAreaId_Should_Return_Empty_List_When_Area_Not_Found(IrasApplicationResponse irasApplicationResponse)
    {
        // Arrange
        var areas = new List<AreaOfChangeDto>
        {
            new() { AutoGeneratedId = "A1", OptionName = "Area 1", SpecificAreasOfChange = [] }
        };
        Sut.TempData[TempDataKeys.ProjectModification.AreaOfChanges] = JsonSerializer.Serialize(areas);
        // Get project record
        Mocker.GetMock<IApplicationsService>()
           .Setup(s => s.GetProjectRecord(It.IsAny<string>()))
           .ReturnsAsync(new ServiceResponse<IrasApplicationResponse> { StatusCode = HttpStatusCode.OK, Content = irasApplicationResponse });

        // Act
        var result = Sut.GetSpecificChangesByAreaId("A2");

        // Assert
        result.ShouldNotBeNull();
        result.Result.ShouldBeOfType<JsonResult>();
    }

    [Theory, AutoData]
    public void GetSpecificChangesByAreaId_Should_Return_SpecificChanges(IrasApplicationResponse irasApplicationResponse)
    {
        // Arrange
        var areas = new List<AreaOfChangeDto>
        {
            new()
            {
                AutoGeneratedId = "A1",
                OptionName = "Area 1",
                SpecificAreasOfChange =
                [
                    new() { AutoGeneratedId = "S1", OptionName = "Spec 1" },
                    new() { AutoGeneratedId = "S2", OptionName = "Spec 2" }
                ]
            }
        };
        Sut.TempData[TempDataKeys.ProjectModification.AreaOfChanges] = JsonSerializer.Serialize(areas);
        // Get project record
        Mocker.GetMock<IApplicationsService>()
           .Setup(s => s.GetProjectRecord(It.IsAny<string>()))
           .ReturnsAsync(new ServiceResponse<IrasApplicationResponse> { StatusCode = HttpStatusCode.OK, Content = irasApplicationResponse });

        // Act
        var result = Sut.GetSpecificChangesByAreaId("A1");

        // Assert
        result.ShouldNotBeNull();
        result.Result.ShouldBeOfType<JsonResult>();
    }

    [Theory, AutoData]
    public void GetSpecificChangesByAreaId_ReturnsSpecificChanges_WhenValidAreaOfChangeId(IrasApplicationResponse irasApplicationResponse)
    {
        // Arrange
        var modificationResponse = new List<GetAreaOfChangesResponse>
        {
            new() {
                Id = "1",
                Name = "Test Area of Change",
                ModificationSpecificAreaOfChanges =
                [
                    new ModificationSpecificAreaOfChangeDto
                    {
                        Id = "1",
                        Name = "Specific Area 1",
                        JourneyType = "specific area 1",
                        ModificationAreaOfChangeId = 1
                    },
                    new ModificationSpecificAreaOfChangeDto
                    {
                        Id = "2",
                        Name = "Specific Area 2",
                        JourneyType = "specific area 2",
                        ModificationAreaOfChangeId = 1
                    }
                ]
            }
        };

        Sut.TempData = new TempDataDictionary(new DefaultHttpContext(), Mock.Of<ITempDataProvider>())
        {
            [TempDataKeys.ProjectModification.AreaOfChanges] = JsonSerializer.Serialize(modificationResponse)
        };
        // Get project record
        Mocker.GetMock<IApplicationsService>()
           .Setup(s => s.GetProjectRecord(It.IsAny<string>()))
           .ReturnsAsync(new ServiceResponse<IrasApplicationResponse> { StatusCode = HttpStatusCode.OK, Content = irasApplicationResponse });

        // Act
        var result = Sut.GetSpecificChangesByAreaId("1");

        // Assert
        var jsonResult = result.Result.ShouldBeOfType<JsonResult>();
        var selectList = jsonResult.Value.ShouldBeOfType<List<SelectListItem>>();

        selectList.Count.ShouldBe(1);
        selectList[0].Text.ShouldBe("Select specific change");
    }

    [Fact]
    public void GetSpecificChangesByAreaId_ReturnsBadRequest_WhenSessionIsMissing()
    {
        // Arrange
        Sut.TempData = new TempDataDictionary(new DefaultHttpContext(), Mock.Of<ITempDataProvider>());
        // Get project record
        Mocker.GetMock<IApplicationsService>()
           .Setup(s => s.GetProjectRecord(It.IsAny<string>()))
           .ReturnsAsync(new ServiceResponse<IrasApplicationResponse> { StatusCode = HttpStatusCode.OK, Content = null });

        // Act
        var result = Sut.GetSpecificChangesByAreaId("123");

        // Assert
        var badRequest = result.Result.ShouldBeOfType<StatusCodeResult>();
        badRequest.StatusCode.ShouldBe(StatusCodes.Status400BadRequest);
    }

    [Fact]
    public void GetSpecificChangesByAreaId_WhenTempDataMissing_ReturnsBadRequest()
    {
        // Arrange
        // Get project record
        Mocker.GetMock<IApplicationsService>()
           .Setup(s => s.GetProjectRecord(It.IsAny<string>()))
           .ReturnsAsync(new ServiceResponse<IrasApplicationResponse> { StatusCode = HttpStatusCode.OK, Content = null });

        var areas = new List<AreaOfChangeDto>
        {
            new()
            {
                AutoGeneratedId = "A1",
                OptionName = "Area 1",
                SpecificAreasOfChange =
                [
                    new() { AutoGeneratedId = "S1", OptionName = "Spec 1" },
                    new() { AutoGeneratedId = "S2", OptionName = "Spec 2" }
                ]
            }
        };
        Sut.TempData[TempDataKeys.ProjectModification.AreaOfChanges] = JsonSerializer.Serialize(areas);

        // Act
        var result = Sut.GetSpecificChangesByAreaId(Guid.NewGuid().ToString());

        // Assert
        result.Result.ShouldBeOfType<JsonResult>();
        result.Result.ShouldNotBeNull();
    }

    [Theory, AutoData]
    public void GetSpecificChangesByAreaId_ProjectHalt_ReturnsOnlyDefaultPlusProjectRestart(IrasApplicationResponse irasApplicationResponse)
    {
        // Arrange
        var (areas, selectedAreaId) = SampleAreas();

        Sut.TempData = new TempDataDictionary(new DefaultHttpContext(), Mock.Of<ITempDataProvider>())
        {
            { TempDataKeys.ProjectModification.AreaOfChanges, Serialize(areas) },
            { TempDataKeys.ProjectRecordStatus, ProjectRecordStatus.ProjectHalt }
        };
        // Get project record
        Mocker.GetMock<IApplicationsService>()
           .Setup(s => s.GetProjectRecord(It.IsAny<string>()))
           .ReturnsAsync(new ServiceResponse<IrasApplicationResponse> { StatusCode = HttpStatusCode.OK, Content = irasApplicationResponse });

        // Act
        var result = Sut.GetSpecificChangesByAreaId(selectedAreaId);

        // Assert
        var list = ExtractJsonSelectList(result.Result);

        list[0].Text.ShouldBe("Select specific change");
        list[0].Value.ShouldBe(Guid.Empty.ToString());

        list[2].Text.ShouldBe("Project restart following temporary halt");
        list[2].Value.ShouldNotBeNullOrEmpty();
    }

    [Fact]
    public void GetSpecificChangesByAreaId_ProjectHalt_MatchingIsCaseInsensitiveAndTrimmed()
    {
        // Arrange: introduce varying casing and whitespace
        var (areas, selectedAreaId) = SampleAreas();

        var target = areas
            .First(a => a.AutoGeneratedId == selectedAreaId)
            .SpecificAreasOfChange
            .First(sc => sc.OptionName.Equals("Project restart following temporary halt", StringComparison.OrdinalIgnoreCase));

        target.OptionName = "Project restart following temporary halt";

        Sut.TempData = new TempDataDictionary(new DefaultHttpContext(), Mock.Of<ITempDataProvider>())
        {
            { TempDataKeys.ProjectModification.AreaOfChanges, Serialize(areas) },
            { TempDataKeys.ProjectRecordStatus, ProjectRecordStatus.ProjectHalt }
        };
        // Get project record
        Mocker.GetMock<IApplicationsService>()
           .Setup(s => s.GetProjectRecord(It.IsAny<string>()))
           .ReturnsAsync(new ServiceResponse<IrasApplicationResponse> { StatusCode = HttpStatusCode.OK, Content = null });

        // Act
        var result = Sut.GetSpecificChangesByAreaId(selectedAreaId);

        // Assert
        var list = ExtractJsonSelectList(result.Result);

        list[2].Text.Trim().ShouldBe("Project restart following temporary halt");
    }

    [Theory, AutoData]
    public void GetSpecificChangesByAreaId_NotProjectHalt_ReturnsAllSpecificItems(IrasApplicationResponse irasApplicationResponse)
    {
        // Arrange
        var (areas, selectedAreaId) = SampleAreas();
        irasApplicationResponse.Status = "Project halt";
        Sut.TempData = new TempDataDictionary(new DefaultHttpContext(), Mock.Of<ITempDataProvider>())
        {
            { TempDataKeys.ProjectModification.AreaOfChanges, Serialize(areas) },
            { TempDataKeys.ProjectRecordStatus, ProjectRecordStatus.ProjectHalt }
        };
        // Get project record
        Mocker.GetMock<IApplicationsService>()
           .Setup(s => s.GetProjectRecord(It.IsAny<string>()))
           .ReturnsAsync(new ServiceResponse<IrasApplicationResponse> { StatusCode = HttpStatusCode.OK, Content = irasApplicationResponse });
        // Act
        var result = Sut.GetSpecificChangesByAreaId(selectedAreaId);

        // Assert
        var list = ExtractJsonSelectList(result.Result);

        // Expect: default select + all 3 specific items
        list[0].Text.ShouldBe("Select specific change");

        var texts = list.Skip(1).Select(i => i.Text).ToList();
        texts.ShouldBe(new[] {
                "Project restart following temporary halt",
        });
    }

    private (List<AreaOfChangeDto> Areas, string SelectedAreaId) SampleAreas()
    {
        var areaId = Guid.NewGuid().ToString();
        var areas = new List<AreaOfChangeDto>
            {
                new()
                {
                    AutoGeneratedId = areaId,
                    OptionName = "Stop or restart",
                    SpecificAreasOfChange = new List<AnswerModel>
                    {
                        new() { AutoGeneratedId = Guid.NewGuid().ToString(), OptionName = "Temporary halt to a project" },
                        new() { AutoGeneratedId = Guid.NewGuid().ToString(), OptionName = "Project restart following temporary halt" },
                        new() { AutoGeneratedId = Guid.NewGuid().ToString(), OptionName = "Other stop or restart of the project" },
                    }
                },
                new()
                {
                    AutoGeneratedId = Guid.NewGuid().ToString(),
                    OptionName = "Something else",
                    SpecificAreasOfChange = new List<AnswerModel>
                    {
                        new() { AutoGeneratedId = Guid.NewGuid().ToString(), OptionName = "Other A" },
                        new() { AutoGeneratedId = Guid.NewGuid().ToString(), OptionName = "Other B" }
                    }
                }
            };

        return (areas, areaId);
    }

    private static List<SelectListItem> ExtractJsonSelectList(IActionResult result)
    {
        result.ShouldBeOfType<JsonResult>();
        var json = (JsonResult)result;
        json.Value.ShouldNotBeNull();
        return ((IEnumerable<SelectListItem>)json.Value!).ToList();
    }

    private static string Serialize(object obj) =>
           JsonSerializer.Serialize(obj);
}